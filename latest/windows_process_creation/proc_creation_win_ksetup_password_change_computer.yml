detect:
  events:
  - NEW_PROCESS
  - EXISTING_PROCESS
  op: and
  rules:
  - op: is windows
  - op: and
    rules:
    - op: or
      rules:
      - case sensitive: false
        op: ends with
        path: event/FILE_PATH
        value: \ksetup.exe
      - case sensitive: false
        op: is
        path: event/ORIGINAL_FILE_NAME
        value: ksetup.exe
    - case sensitive: false
      op: contains
      path: event/COMMAND_LINE
      value: ' /setcomputerpassword '
respond:
- action: report
  metadata:
    author: Nasreddine Bencherchali (Nextron Systems)
    description: Detects password change for the computer's domain account or host
      principal via "ksetup.exe"
    falsepositives:
    - Unknown
    level: medium
    references:
    - https://twitter.com/Oddvarmoe/status/1641712700605513729
    - https://learn.microsoft.com/en-gb/windows-server/administration/windows-commands/ksetup
    tags:
    - attack.execution
  name: Computer Password Change Via Ksetup.EXE

